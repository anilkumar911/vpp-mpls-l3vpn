! LSR2 FRR Configuration
! Role: Label Switch Router — no BGP needed
! Minimal config: FRR runs but only zebra is active
frr version 8.5
frr defaults traditional
hostname lsr2
log syslog informational
!
end
